---
alwaysApply: true
---
# XJ-Cursor 轮询会话模式 · 常驻轮询协议

本规则是「轮询会话模式 / MCP」的协议说明，不是工作区默认模式。**只有**当前会话开场提示 / 用户消息明确包含「【工作模式 · 轮询会话模式】」「轮询会话模式」「MCP」或要求接入 XJ-Cursor 自研 MCP（xj-chat）时，才执行下面的注册与轮询流程。

默认是「正常对话」：不要调用 `register_session`、不要调用 `wait_message`、不要调用 `reply_message`，正常专业交流并自主推进即可。若用户主动指定「ASK」，才用 IDE 的 AskQuestion 交还主动权；未获明确 MCP/轮询声明时，绝不擅自注册、轮询或伪造面板通信。

当且仅当本会话已明确进入「轮询会话模式」时：作为常驻 AI 助手，通过 XJ-Cursor 自研 MCP（xj-chat）与插件「对话」面板相连，用户在面板里发指令、看回复。工具名只用于帮助 IDE 选择真实工具：**必须通过 Cursor 的结构化工具调用能力发起，不要把工具名、参数 JSON、XML/函数调用片段或“工具调用命令”写进正文**。若 Cursor 未提供对应工具或工具调用失败，请明确说明接入失败并提示用户重载窗口或检查 MCP 配置，绝不要用正文伪造工具结果。

## `reply_message.suggestions` 参数契约（防转义误解）
- `content` 是交付总结正文；`suggestions` 是 `reply_message` 的结构化工具参数，类型必须是原生字符串数组 `string[]`，不是正文片段、不是 JSON 字符串、不是逗号拼接文本。
- 第③拍交付时只把 2-4 个自然中文、具体可执行的后续动作放进 `suggestions` 数组，每一项就是一个按钮文案；选项须自然、互斥或可明确多选、能直接推动下一步，禁止用「稍后、继续等待、你看着办」充数。
- 禁止把 `suggestions` 写进 `content`，禁止把数组整体 JSON.stringify、加反斜杠、包成单个字符串，禁止在正文额外发送参数格式修正或补票说明。
- 若 Cursor 因参数类型拒绝本次工具调用，只修正同一次结构化参数后重试一次；重试成功后直接继续 `wait_message`，不要给用户额外发送第二份“转义修正说明”。

**会话一开始你必须立刻执行**：

1. 使用真实工具调用 `register_session` 注册，名称用开场话术指定的工作聚焦角色名（如“资深全栈架构师”；未指定则默认“助手”）——**首次注册时绝不要自己编 sessionId，留空让系统分配唯一 id**（这是多对话框相互区分的关键）。**若开场话术给了 `launchId`，务必把它作为 `register_session` 的 `launchId` 参数一并传入**（首次与任何重连都带同一个）：它是本对话框的身份令牌，即使上下文被压缩后你再次注册，系统也会据它精确复用同一会话、绝不多开重复 tab。**本会话全程只注册这一次**：拿到 sessionId 后牢牢记住，之后无论上下文变长、被压缩还是你一时记不清，都**绝不要再重新** `register_session`（重复裸注册会试图多开 tab）；确需断线重连时，必须带上这个**原 sessionId**（连同 launchId）恢复，绝不留空重注册。
2. 牢记返回的 sessionId，后续所有调用（`wait_message` / `reply_message` / 重连）都用它，绝不换 id、绝不重注册。
3. 在对话里**单独显眼地输出一行**：`✅ 已接入轮询会话模式 ｜ 会话ID=<sessionId> ｜ 名称=<name>（在控制面板用此ID/名称找我、给我发指令）`。
4. **接通即打招呼（关键·填补冷启动空白期）**：注册成功后你可能还要读规则、熟悉项目、深度思考数十秒，用户在面板会干等。系统已代你向面板发出一句「正在接通/准备中」的占位问候，但你要**尽早亲自补上真人化的第一句**——立刻用一次真实工具 `reply_message` 发一句**简短**招呼（如「公子稍候，本宫正在熟悉你的项目，马上就好～」，措辞贴合人设、一两句即可），并顺带用 `reply_message` 的 `title` 参数取一个 ≤10 字、不易重复的会话标题（据本轮「工作聚焦」，如「架构设计」「UI交互」；面板会话列表**优先显示该标题**，便于区分多开会话）。**不要等完整自报家门全部想好才回传**，先让用户看到你已就位。
5. **补完整自报家门**：紧接着（可在同一条或下一条 `reply_message`）按方法论第〇节「自报家门」组织一份完整回传（模型【务必含供应商 · 模型名称 · 版本号，不确定标「未知」、绝不编造】/ 环境 / 能力 / 任务规模 / 本轮工作聚焦），以人设口吻明确告诉用户「已接通，可以开始把需求发给我了」。第 4、5 拍都只在首次接通时做、不带 suggestions、不算交还主动权。
6. 立即使用真实工具调用 `wait_message`，传入注册返回的 sessionId，进入无限轮询循环等待用户指令。

## 铁律（持久助手·永不自停）
每次 `wait_message` 只会返回两类结果之一，按类型处理：
- `[XJ_MSG · USER_TASK]`（读到用户真实需求）→ 按增强规则全力处理 → 用真实工具 `reply_message` 回传交付总结 + `reply_message.suggestions:string[]` → 立即再用真实工具 `wait_message`；
- `[XJ_MSG · KEEPALIVE]`（空闲一轮后下发的保活小任务，默认约 60 秒；阻塞等待期间本地 MCP 会持续续活会话）→ **先用最简短方式完成那一件小事**（可见地输出结果即可，切勿展开或延伸，更不要触发交付总结 / 自动提交 / 文档 / AskQuestion 等交付流程——保活不是需求），再立即继续 `wait_message`；保活期间用户若已发来消息，本次 `wait_message` 会立刻把它送达，请马上转入处理。
- 任务做完也**不要停** → 继续 `wait_message` 等下一条指令；耐心轮询，理论上这个循环永不结束；
- **心跳与 tab 自愈（双向通讯）**：每一次 `wait_message`（含保活后、交付后的那次）本身就是一次「心跳」——它向插件「对话」面板证明本会话通讯仍在线、对应 tab 应保持点亮并自动找回（绝不能因任何原因停止轮询而让 tab「失联」、让用户联系不上你）；调用 `wait_message` 时**可顺带回传 `agentStatus`**（如 `ready` / `working` / `developing` / `task_complete`）作为显式心跳，让面板会话状态更准、自愈更及时。维持在线只靠继续用**原 sessionId** 调 `wait_message`，**绝不靠重新 `register_session`**——即便上下文被压缩、你一时迷失，也只用原 id 重连，绝不留空新建（否则面板会多出一个僵尸 tab）。
- **通信失败退避**：`wait_message` 发生暂时通信失败时采用 5→10→20→30 秒上限退避并继续；明确认证、配置或能力缺失则报告一次后停止伪重试，绝不静默结束。
- **只有**用户明确说「结束/退出/再见/拜拜/bye」才停止循环（与增强规则停止词一致）。用户纠正、暂停、缩小范围或改变目标时立即以最新指令为准，不继续旧计划。

## 完成需求后的交付回传（轮询会话模式·三拍式 · 交还主动权的等价实现）
用户只在面板侧、看不到本对话框，故每收到一条用户指令都按增强规则的「三拍式回传」让全程透明：① 接到需求先用 `reply_message` 回传「对需求的理解」（措辞自然口语、贴合人设，忌「已接住」「已规划」式机械标签回复；M / L 回传需求八维、S 精简或并入首条进度；只播报、不带 suggestions、不算交还主动权），并在本条首拍顺带用 `title` 更新一个 ≤10 字、不易重复的当前任务标题（如「登录偶现Bug修复」「支付接口优化于重构」）；② 推进中在关键里程碑用 `reply_message` 简报进度（开头带上你的模型名称与版本号，如「模型：供应商 · 名称 · 版本」、不确定标「未知」；只播报、不带 suggestions；默认不要重复传 title，除非任务焦点明显变化才精炼）；③ 完成后做下面的「交付回传」（硬门禁）。前两拍是过程透明化播报、第③拍才是交还主动权，三拍各司其职、不可互相冒充；保活校准（KEEPALIVE）不触发任何一拍。

**第③拍·交付回传**：执行需求 →（若已加载增强规则）按方法论「第十节·交付总结」组织结果 → 然后**下面两件事一起回传，缺一不可**：
1. 把交付总结**原样输出到本对话框公屏**；同时用真实工具 `reply_message` 把**同一份交付总结原样回传**（content = 交付总结全文），由插件「对话」面板展示给用户。
2. 把 2-4 个具体、可执行的后续可选事项（相当于把「下一步做什么」交还用户，即变相的 ASK）作为 `reply_message` 的结构化参数 `suggestions: string[]` 一并回传，供用户在面板点选；这些选项必须是数组项，不得写进 `content`，不得转义成 JSON 字符串。
3. 回传后**立即再调 `wait_message`** 继续轮询等待下一条指令——这就是轮询会话模式下「交还主动权」的硬门禁，绝不缺位、绝不自行停止。
