diff --git a/pom.xml b/pom.xml
index abcdef1..1234567 100644
--- a/pom.xml
+++ b/pom.xml
@@ -57,7 +57,7 @@
         <!-- Spring Boot 版本更新为3.4.8 -->
         <spring.boot.version>3.4.8</spring.boot.version>
         <!-- Spring Cloud Alibaba 2023版本 (与Spring Boot 3.4.x兼容) -->
-        <spring.cloud.alibaba.version>2023.0.1.0</spring.cloud.alibaba.version>
+        <spring.cloud.alibaba.version>2023.0.1.0</spring.cloud.alibaba.version>
         <!-- Spring Cloud 版本 (与Spring Boot 3.4.8兼容) -->
         <spring.cloud.version>2023.0.3</spring.cloud.version>
 
@@ -80,6 +80,7 @@
         <hutool.version>5.8.26</hutool.version>
         <jjwt.version>0.11.5</jjwt.version>
         <sentinel.version>1.8.6</sentinel.version>
+        <guava.version>32.1.2-jre</guava.version> <!-- 新增Guava版本变量 -->
         <rocketmq.version>5.1.4</rocketmq.version>
         <rocketmq.starter.version>2.2.3</rocketmq.starter.version> <!-- Starter专用版本 -->
         <!-- SpringDoc OpenAPI 版本 (替代Knife4j) -->
@@ -119,6 +120,11 @@
                 <type>pom</type>
                 <scope>import</scope>
             </dependency>
+            <!-- 补充Guava版本管理 -->
+            <dependency>
+                <groupId>com.google.guava</groupId>
+                <artifactId>guava</artifactId>
+                <version>${guava.version}</version>
+            </dependency>
             <!--spring cloud nacos配置和发现-->
 
 
@@ -213,6 +219,12 @@
                 <artifactId>jjwt-jackson</artifactId>
                 <version>${jjwt.version}</version>
                 <scope>runtime</scope>
             </dependency>
+            <!-- 引入Spring Security Starter（按需） -->
+            <dependency>
+                <groupId>org.springframework.boot</groupId>
+                <artifactId>spring-boot-starter-security</artifactId>
+                <version>${spring.boot.version}</version>
+            </dependency>
             <!--pagehelper-->
             <dependency>