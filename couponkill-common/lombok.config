# 将字段上的 Jackson 注解复制到 Lombok 生成的 getter/setter，
# 否则 @JsonSerialize(ToStringSerializer) 仅打在 private 字段上时不会生效。
config.stopBubbling = true
lombok.copyableAnnotations += com.fasterxml.jackson.databind.annotation.JsonSerialize
lombok.copyableAnnotations += com.fasterxml.jackson.annotation.JsonFormat
