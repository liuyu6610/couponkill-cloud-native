<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.seckill.couponkillcouponservice.mapper.CouponMapper">

    <resultMap id="CouponMap" type="com.aliyun.seckill.common.pojo.Coupon">
        <id column="id" property="id"/>
        <id column="shard_index" property="shardIndex"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="type" property="type"/>
        <result column="face_value" property="faceValue"/>
        <result column="min_spend" property="minSpend"/>
        <result column="valid_days" property="validDays"/>
        <result column="per_user_limit" property="perUserLimit"/>
        <result column="total_stock" property="totalStock"/>
        <result column="seckill_total_stock" property="seckillTotalStock"/>
        <result column="remaining_stock" property="remainingStock"/>
        <result column="seckill_remaining_stock" property="seckillRemainingStock"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="version" property="version"/>
    </resultMap>

    <insert id="insertCoupon">
        INSERT INTO coupon (
            id, name, description, type, face_value, min_spend, valid_days,
            per_user_limit, total_stock, seckill_total_stock, remaining_stock,
            seckill_remaining_stock, status, create_time, update_time, version, shard_index
        ) VALUES (
                     #{id}, #{name}, #{description}, #{type}, #{faceValue}, #{minSpend}, #{validDays},
                     #{perUserLimit}, #{totalStock}, #{seckillTotalStock}, #{remainingStock},
                     #{seckillRemainingStock}, #{status}, #{createTime}, #{updateTime}, #{version}, #{shardIndex}
                 )
    </insert>

    <delete id="deleteCouponById">
        DELETE FROM coupon WHERE id = #{id}
    </delete>

    <select id="selectAvailableCoupons" resultMap="CouponMap">
        SELECT * FROM coupon 
        WHERE status = 1 AND create_time >= DATE_SUB(NOW(), INTERVAL valid_days DAY) 
        GROUP BY id, shard_index
        ORDER BY create_time DESC
        LIMIT 1000
    </select>

    <select id="selectAll" resultMap="CouponMap">
        SELECT * FROM coupon 
        GROUP BY id, shard_index
        ORDER BY create_time DESC
        LIMIT 1000
    </select>

    <select id="selectById" resultMap="CouponMap">
        SELECT * FROM coupon 
        WHERE id = #{id} 
        GROUP BY id, shard_index
        LIMIT 1
    </select>

    <!-- 根据优惠券ID查询所有分片，优化查询性能 -->
    <select id="selectByCouponId" resultMap="CouponMap">
        SELECT id, shard_index, name, description, type, face_value, min_spend, valid_days,
               per_user_limit, total_stock, seckill_total_stock, remaining_stock,
               seckill_remaining_stock, status, create_time, update_time, version
        FROM coupon 
        WHERE id = #{couponId} 
        ORDER BY shard_index
    </select>

    <!-- 查询所有优惠券的主分片（shard_index=0）用于预热 -->
    <select id="selectMainShardsForCache" resultMap="CouponMap">
        SELECT id, shard_index, name, description, type, face_value, min_spend, valid_days,
               per_user_limit, total_stock, seckill_total_stock, remaining_stock,
               seckill_remaining_stock, status, create_time, update_time, version
        FROM coupon 
        WHERE shard_index = 0 AND status = 1
        ORDER BY create_time DESC
        LIMIT 1000
    </select>

    <update id="updateStockByShardIndex">
        UPDATE coupon
        SET seckill_remaining_stock = seckill_remaining_stock + #{change},
            version = version + 1,
            update_time = NOW()
        WHERE id = #{couponId} AND shard_index = #{shardIndex} AND version = #{version}
          AND seckill_remaining_stock + #{change} >= 0
    </update>

    <update id="updateStock">
        UPDATE coupon
        SET remaining_stock = remaining_stock + #{change},
            version = version + 1,
            update_time = NOW()
        WHERE id = #{couponId} AND version = #{version}
          AND remaining_stock + #{change} >= 0
    </update>

    <select id="selectExpiredCoupons" resultMap="CouponMap">
        SELECT * FROM coupon
        WHERE status = 1 AND create_time &lt;= #{expireTime} 
        GROUP BY id, shard_index
        LIMIT 1000
    </select>

    <update id="updateCouponStatus">
        UPDATE coupon
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{couponId}
    </update>

</mapper>