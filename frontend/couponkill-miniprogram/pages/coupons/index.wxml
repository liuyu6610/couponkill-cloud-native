<view class="page">
  <view wx:if="{{isLoading}}" class="hint">加载中...</view>
  <block wx:for="{{coupons}}" wx:key="id">
    <view class="card" data-id="{{item.id}}" bindtap="goDetail">
      <view class="name">{{item.name}}</view>
      <view class="meta">
        {{item.type==2 ? '秒杀' : '常驻'}} · ¥{{item.faceValue}}
      </view>
    </view>
  </block>
  <view wx:if="{{!isLoading && coupons.length===0}}" class="hint">暂无可用优惠券</view>
</view>
