---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: seckills.ops.couponkill.io
spec:
  group: ops.couponkill.io
  names:
    kind: Seckill
    listKind: SeckillList
    plural: seckills
    singular: seckill
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Seckill is the Schema for the seckills API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SeckillSpec defines the desired state of Seckill
            properties:
              monitoring:
                description: Monitoring defines the monitoring configuration
                properties:
                  enabled:
                    description: Enabled indicates whether monitoring is enabled
                    type: boolean
                  grafanaEnabled:
                    description: GrafanaEnabled indicates whether Grafana monitoring
                      is enabled
                    type: boolean
                  prometheusEnabled:
                    description: PrometheusEnabled indicates whether Prometheus monitoring
                      is enabled
                    type: boolean
                type: object
              scaling:
                description: Scaling defines the autoscaling configuration
                properties:
                  hpa:
                    description: HPA defines the HorizontalPodAutoscaler configuration
                    properties:
                      enabled:
                        description: Enabled indicates whether HPA is enabled
                        type: boolean
                      maxReplicas:
                        description: MaxReplicas is the maximum number of replicas
                        format: int32
                        type: integer
                      minReplicas:
                        description: MinReplicas is the minimum number of replicas
                        format: int32
                        type: integer
                      targetCPUUtilizationPercentage:
                        description: TargetCPUUtilizationPercentage is the target
                          CPU utilization percentage
                        format: int32
                        type: integer
                    type: object
                  keda:
                    description: KEDA defines the KEDA configuration
                    properties:
                      enabled:
                        description: Enabled indicates whether KEDA is enabled
                        type: boolean
                      kafkaScaler:
                        description: KafkaScaler defines the Kafka scaler configuration
                        properties:
                          bootstrapServers:
                            description: BootstrapServers is the Kafka bootstrap
                              servers
                            type: string
                          consumerGroup:
                            description: ConsumerGroup is the Kafka consumer group
                            type: string
                          lagThreshold:
                            description: LagThreshold is the lag threshold
                            type: string
                          topic:
                            description: Topic is the Kafka topic
                            type: string
                        type: object
                    type: object
                type: object
              services:
                description: Services defines the configuration for all microservices
                properties:
                  couponService:
                    description: CouponService defines the configuration for the
                      coupon service
                    properties:
                      enabled:
                        description: Enabled indicates whether the service is enabled
                        type: boolean
                      env:
                        description: Environment variables for the service
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container
                          properties:
                            name:
                              description: Name of the environment variable
                              type: string
                            value:
                              description: Value of the environment variable
                              type: string
                            valueFrom:
                              description: ValueFrom defines a source for the environment
                                variable's value
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the ConfigMap to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                secretKeyRef:
                                  description: Selects a key of a Secret
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the Secret to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image defines the container image for the service
                        type: string
                      ports:
                        description: Ports defines the ports exposed by the service
                        items:
                          description: ServicePort describes the port exposed by
                            a service
                          properties:
                            name:
                              description: Name for the port
                              type: string
                            port:
                              description: Port number
                              format: int32
                              type: integer
                            protocol:
                              description: Protocol for the port
                              type: string
                            targetPort:
                              description: TargetPort number
                              format: int32
                              type: integer
                          type: object
                        type: array
                      replicas:
                        description: Replicas is the desired number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources defines the resource requirements
                          for the service
                        properties:
                          limits:
                            additionalProperties:
                              type: string
                            description: Limits describes the maximum amount of
                              compute resources allowed
                            type: object
                          requests:
                            additionalProperties:
                              type: string
                            description: Requests describes the minimum amount of
                              compute resources required
                            type: object
                        type: object
                    type: object
                  gatewayService:
                    description: GatewayService defines the configuration for the
                      gateway service
                    properties:
                      enabled:
                        description: Enabled indicates whether the service is enabled
                        type: boolean
                      env:
                        description: Environment variables for the service
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container
                          properties:
                            name:
                              description: Name of the environment variable
                              type: string
                            value:
                              description: Value of the environment variable
                              type: string
                            valueFrom:
                              description: ValueFrom defines a source for the environment
                                variable's value
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the ConfigMap to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                secretKeyRef:
                                  description: Selects a key of a Secret
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the Secret to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image defines the container image for the service
                        type: string
                      ports:
                        description: Ports defines the ports exposed by the service
                        items:
                          description: ServicePort describes the port exposed by
                            a service
                          properties:
                            name:
                              description: Name for the port
                              type: string
                            port:
                              description: Port number
                              format: int32
                              type: integer
                            protocol:
                              description: Protocol for the port
                              type: string
                            targetPort:
                              description: TargetPort number
                              format: int32
                              type: integer
                          type: object
                        type: array
                      replicas:
                        description: Replicas is the desired number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources defines the resource requirements
                          for the service
                        properties:
                          limits:
                            additionalProperties:
                              type: string
                            description: Limits describes the maximum amount of
                              compute resources allowed
                            type: object
                          requests:
                            additionalProperties:
                              type: string
                            description: Requests describes the minimum amount of
                              compute resources required
                            type: object
                        type: object
                    type: object
                  goService:
                    description: GoService defines the configuration for the Go service
                    properties:
                      enabled:
                        description: Enabled indicates whether the service is enabled
                        type: boolean
                      env:
                        description: Environment variables for the service
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container
                          properties:
                            name:
                              description: Name of the environment variable
                              type: string
                            value:
                              description: Value of the environment variable
                              type: string
                            valueFrom:
                              description: ValueFrom defines a source for the environment
                                variable's value
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the ConfigMap to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                secretKeyRef:
                                  description: Selects a key of a Secret
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the Secret to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image defines the container image for the service
                        type: string
                      ports:
                        description: Ports defines the ports exposed by the service
                        items:
                          description: ServicePort describes the port exposed by
                            a service
                          properties:
                            name:
                              description: Name for the port
                              type: string
                            port:
                              description: Port number
                              format: int32
                              type: integer
                            protocol:
                              description: Protocol for the port
                              type: string
                            targetPort:
                              description: TargetPort number
                              format: int32
                              type: integer
                          type: object
                        type: array
                      replicas:
                        description: Replicas is the desired number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources defines the resource requirements
                          for the service
                        properties:
                          limits:
                            additionalProperties:
                              type: string
                            description: Limits describes the maximum amount of
                              compute resources allowed
                            type: object
                          requests:
                            additionalProperties:
                              type: string
                            description: Requests describes the minimum amount of
                              compute resources required
                            type: object
                        type: object
                    type: object
                  orderService:
                    description: OrderService defines the configuration for the order
                      service
                    properties:
                      enabled:
                        description: Enabled indicates whether the service is enabled
                        type: boolean
                      env:
                        description: Environment variables for the service
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container
                          properties:
                            name:
                              description: Name of the environment variable
                              type: string
                            value:
                              description: Value of the environment variable
                              type: string
                            valueFrom:
                              description: ValueFrom defines a source for the environment
                                variable's value
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the ConfigMap to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                secretKeyRef:
                                  description: Selects a key of a Secret
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the Secret to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image defines the container image for the service
                        type: string
                      ports:
                        description: Ports defines the ports exposed by the service
                        items:
                          description: ServicePort describes the port exposed by
                            a service
                          properties:
                            name:
                              description: Name for the port
                              type: string
                            port:
                              description: Port number
                              format: int32
                              type: integer
                            protocol:
                              description: Protocol for the port
                              type: string
                            targetPort:
                              description: TargetPort number
                              format: int32
                              type: integer
                          type: object
                        type: array
                      replicas:
                        description: Replicas is the desired number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources defines the resource requirements
                          for the service
                        properties:
                          limits:
                            additionalProperties:
                              type: string
                            description: Limits describes the maximum amount of
                              compute resources allowed
                            type: object
                          requests:
                            additionalProperties:
                              type: string
                            description: Requests describes the minimum amount of
                              compute resources required
                            type: object
                        type: object
                    type: object
                  userService:
                    description: UserService defines the configuration for the user
                      service
                    properties:
                      enabled:
                        description: Enabled indicates whether the service is enabled
                        type: boolean
                      env:
                        description: Environment variables for the service
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container
                          properties:
                            name:
                              description: Name of the environment variable
                              type: string
                            value:
                              description: Value of the environment variable
                              type: string
                            valueFrom:
                              description: ValueFrom defines a source for the environment
                                variable's value
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the ConfigMap to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                secretKeyRef:
                                  description: Selects a key of a Secret
                                  properties:
                                    key:
                                      description: The key to select
                                      type: string
                                    name:
                                      description: The name of the Secret to select
                                        from
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image defines the container image for the service
                        type: string
                      ports:
                        description: Ports defines the ports exposed by the service
                        items:
                          description: ServicePort describes the port exposed by
                            a service
                          properties:
                            name:
                              description: Name for the port
                              type: string
                            port:
                              description: Port number
                              format: int32
                              type: integer
                            protocol:
                              description: Protocol for the port
                              type: string
                            targetPort:
                              description: TargetPort number
                              format: int32
                              type: integer
                          type: object
                        type: array
                      replicas:
                        description: Replicas is the desired number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources defines the resource requirements
                          for the service
                        properties:
                          limits:
                            additionalProperties:
                              type: string
                            description: Limits describes the maximum amount of
                              compute resources allowed
                            type: object
                          requests:
                            additionalProperties:
                              type: string
                            description: Requests describes the minimum amount of
                              compute resources required
                            type: object
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: SeckillStatus defines the observed state of Seckill
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of a Seckill's current state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase of the Seckill
                type: string
              serviceStatuses:
                additionalProperties:
                  description: ServiceStatus defines the status of a service
                  properties:
                    phase:
                      description: Phase is the current phase of the service
                      type: string
                    readyReplicas:
                      description: ReadyReplicas is the number of ready replicas
                      format: int32
                      type: integer
                    totalReplicas:
                      description: TotalReplicas is the total number of replicas
                      format: int32
                      type: integer
                  type: object
                description: ServiceStatuses contains the status of each service
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
