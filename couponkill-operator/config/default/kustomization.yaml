# Adds namespace to all resources.
namespace: couponkill-operator-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: couponkill-operator-

# Labels to add to all resources and selectors.
#labels:
#- includeSelectors: true
#  pairs:
#    someName: someValue

resources:
  - ../crd
  - ../rbac
  - ../manager
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix including the one in
# crd/kustomization.yaml
#- ../webhook
# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix including the one in
# crd/kustomization.yaml
#- ../certmanager
# [PROMETHEUS] To enable prometheus monitor, uncomment all the sections with [PROMETHEUS] prefix
#- ../prometheus

patches:
  # Protect the /metrics endpoint by putting it behind auth.
  # If you want your controller-manager to expose the /metrics
  # endpoint w/o any authn/z, please comment the following line.
  - path: manager_auth_proxy_patch.yaml

# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix including the one in
# crd/kustomization.yaml
#- path: manager_webhook_patch.yaml

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix including the one in
# crd/kustomization.yaml
#- path: webhookcainjection_patch.yaml

# [PROMETHEUS] To enable prometheus monitor, uncomment all the sections with [PROMETHEUS] prefix
#- path: monitor.yaml

# the following config is for teaching kustomize how to do var substitution
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
